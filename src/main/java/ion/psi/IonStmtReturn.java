package ion.psi;

import com.intellij.lang.ASTNode;
import org.jetbrains.annotations.NotNull;

public class IonStmtReturn extends IonPsiElement {
  public IonStmtReturn(@NotNull ASTNode node) {
    super(node);
  }
}
