package ion.psi;

import com.intellij.lang.ASTNode;
import org.jetbrains.annotations.NotNull;

public class IonStmtLabel extends IonDeclBase {
  public IonStmtLabel(@NotNull ASTNode node) {
    super(node);
  }
}
