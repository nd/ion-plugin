package ion.psi;

import com.intellij.lang.ASTNode;
import org.jetbrains.annotations.NotNull;

public class IonType extends IonPsiElement {
  public IonType(@NotNull ASTNode node) {
    super(node);
  }
}
